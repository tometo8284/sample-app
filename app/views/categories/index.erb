<h2>カテゴリ一覧</h2>
<%= form_with model: @category do |f| %>
<p>ジャンル名</p>
  <%= f.text_field :name %>
  <%= f.submit '登録' %>
<% end %>

<% @categories.each do |category| %>
  <p><%= category.name %></p>
  <p><%= link_to "編集", edit_category_path(category.id) %></p>
<% end %>