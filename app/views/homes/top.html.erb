<h1 class="jquery">トップページ</h1>
<%= link_to '新規投稿', new_list_path %>
<%= link_to '投稿一覧', lists_path %>


<script>
  let alertrString;
  alertrString = addString("webcamp")
  alert(alertrString)
  function addString(strA) {
    let addStr = "hello" + strA;
    return addStr;
  }
  let promptStr = prompt('何か好きな文字を入力してください。');
alert(promptStr);

</script>